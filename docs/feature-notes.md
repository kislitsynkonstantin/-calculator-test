# Заметка: планы по новым фичам

Сюда Константин складывает идеи по новым возможностям калькулятора, а я —
предложения на рассмотрение. Это не план работ и не обещание сроков: список
идей, из которого берут следующую задачу.

**Как пользуемся.** Константин пишет «добавь в заметку …» — я записываю сюда
и больше ничего не делаю, пока он не скажет начинать. Спрашивает «что есть в
заметке» — я читаю этот файл и отвечаю по нему, а не по памяти.

**Выполненное из лога убирается.** Очередь — это то, что ещё не сделано; сделанное
живёт в журнале обновлений. Исключение — справка и руководство: про них в журнал не
пишут, поэтому выполненная задача по справке просто исчезает из очереди.
Установлено Константином 30.08.2026.

**Где это ещё лежит.** С 30.08.2026 тот же список стоит главой «Лог задач» в справке
калькулятора — видна администратору и редактору, менеджерам нет. Файл и глава правятся
вместе, одним коммитом: файл подробный, глава тезисная.

**Метки состояния:**

- `ждёт` — записано, к работе не приступали
- `обсуждаем` — вопросы заданы, ждём решения Константина
- `отложено` — решили вернуться позже, из очереди убрано
- `в работе` — делается сейчас
- `отклонено` — решили не делать; причина записана

Технические планы под конкретную работу живут отдельно:
`kb-calculator-plan.md`, `calculator-security-plan.md`,
`calculator-pricing-migration-plan.md`. Здесь — только идеи.

---

## От Константина

### Покраска: терраса, пергола и ограждения · `ждёт`

Марки с оговоркой «или аналоги» прописаны 02.09.2026 — осталась вторая половина.

Записано 29.08.2026. Две правки в разделе «7. Покраска», обе по чек-листу.

**Марки и «аналоги».** Марки в опциях названы: Tikkurila, Osmo, G-Nature,
Sikkens, Zobel. Чего нет — оговорки «или аналоги» ни в одной из них. Нужно
прописать по чек-листу: тип краски и допустимая замена.

**Терраса, пергола, ограждения.** Терраса в опциях есть — «Покраска террасы в
2 слоя маслом G-Nature (за 1 м²)» и «Обработка террасной доски специальным
маслом». Ни перголы, ни ограждений террасы в покраске нет вовсе: из
«ограждений» в опциях только «Ограждение для печи из липы», это другое.
При этом примечание раздела «Дополнительные» прямо говорит, что ограждения,
пергола и ступени в базу не входят, — то есть их красят, а посчитать нечем.

### База знаний для менеджеров — заполнить · `отложено`

Записано 29.08.2026.

Отложено Константином 29.08.2026 — вернуться позже.

«База знаний» — кнопка в шапке и клавиша «Б». Это **отдельный сайт**
`knowledge.baniamsk.ru`, который открывается окном внутри калькулятора. Значит
работа тут не в коде калькулятора, а в наполнении того сайта: правки в
`index.html` не потребуются, если не менять сам способ показа.

Из контейнера содержимое базы знаний не видно — домен закрыт исходящим прокси,
поэтому что именно там не дописано, определить смогу только с твоих слов или
по списку разделов, который ты пришлёшь.

### Раздел «Визуализация и планировка» — переработать · `ждёт`

Записано 29.08.2026.

Три задачи в одном разделе:

1. **Переработать сам раздел.** Он сложился как холст для картинок и с тех пор
   не менялся.
2. **Вывод на печать отдельным файлом.** Сейчас изображения печатаются только
   вместе со спецификацией. Нужна возможность отправить их на печать отдельно —
   своим документом.
3. **Разделить поля под визуализацию и под планировку.** Сейчас это одно поле,
   и картинки обоих видов лежат вперемешку.

Что уже есть в коде под это — чтобы не начинать с нуля:

- Раздел живёт в карточке `#imageCanvasCard` с заголовком «Визуализация и
  планировка». Внутри один холст `#imageCanvas`: перетаскивание файлов,
  «Добавить изображение», «Очистить всё», «Авто-расстановка».
- Порядок и состав картинок хранит `canvasItems`. Разделение на два поля упрётся
  именно в него: сейчас это один список без признака вида.
- Переключатель «В печати: вкл/выкл» (`togglePrintImages`) уже умеет убирать
  картинки из документа целиком. Для печати отдельным файлом он станет выбором
  из трёх состояний, а не двух.
- Готовый блок картинок для печати собирает `imgHtml` в `buildPrintDoc()` — он
  уже вставляется в пять стилей из шести. Отдельный документ сможет взять тот же
  блок, а не собирать разметку заново.
- Картинки уезжают в облако через `uploadCanvasToStorage()` и лежат в снимке
  расчёта как `canvasImages`. При разделении на два поля снимок и восстановление
  придётся править согласованно, иначе сохранённые расчёты потеряют раскладку.

Вопросы, на которые нужен ответ до начала работы, — в конце файла.

### Договор в калькуляторе и согласование пресета · `ждёт`

Записано 05.09.2026 со слов Константина: внедрить договор в калькуляторе и функцию
согласования пресета, после которого открывается доступ на договор.

Две части:

1. **Договор в калькуляторе.** Довести до рабочего состояния и выпустить.
2. **Согласование пресета.** Пресет проходит согласование, и только после него
   открывается доступ к договору.

Что уже есть в коде под первую часть — её начинать не с нуля:

- Текст договора подряда собран целиком: `buildContractHtmlKar(vars)` — предмет,
  участок, объект, сроки, оплата, ответственность, реквизиты сторон. Поля подставляются
  через объект `vars`.
- Данные для подстановки собирает `getContractVarsKar()`: заказчик, участок,
  кадастровый номер, размеры, срок в рабочих днях, итог, НДС, строки платёжного плана.
- Реквизиты вводятся в своём окне — `openContractDataModal()`, кнопка `#btnContractData`.
  Есть проверка заполненности с диалогом «Открыть реквизиты».
- Предпросмотр в окне печати — вкладка «Договор», `renderContractPreviewKarDom()`,
  печать `printContractKar()` через скрытый iframe формата A4.
- Приставка к номеру договора по составу разделов («-КАР», «-КБ», «-ОТД») уже считается
  и держится при снятой галочке раздела.

Под вторую часть готового нет ничего: у пресетов (`presets` в базе, `savePreset()`)
признака согласования не заведено, они личные — привязаны к `user_id`. Роли в системе
есть: `admin`, `editor`, `specialist`, менеджер.

**Вопросы, на которые нужен ответ до начала работы:**

- Что значит «внедрить договор» при том, что текст, реквизиты, предпросмотр и печать
  уже собраны, — довести и выпустить как есть, или там чего-то не хватает?
- Кто согласует пресет — администратор, редактор, кто-то ещё? Один человек или любой
  из роли?
- Что открывается после согласования: вкладка «Договор» в окне печати, печать договора,
  или и то и другое?
- Согласованный пресет изменили — согласование слетает и его надо получать заново?
- Пресеты сейчас личные (`user_id`). Согласующий должен видеть чужой пресет — значит
  доступ к чужим пресетам придётся открывать. Как именно?

## Отклонено

Убрано из очереди Константином 29.08.2026. Строки остаются здесь, чтобы не
предлагать заново. **В справке этого списка больше нет** — убран из главы «Лог
задач» по указанию Константина 04.09.2026: очередь показывает то, что осталось
сделать, а отклонённое ему в интерфейсе не нужно. Запись живёт только в этом файле.

- **История цен: кто и когда менял цифры** — данные в `pricing_history` лежат,
  показывать пока не будем.
- **Сравнение двух расчётов рядом.**
- **Статус расчёта: черновик → отправлен → согласован** — закроется журналом
  действий менеджеров.
- **Шаблоны примечаний.**
- **Поиск по клиенту и договору.**
- **Напоминание о сроке действия спец. цены.**

---

## Вопросы, на которые нужен ответ

### По разделу «Визуализация и планировка»

1. **Что печатать отдельным файлом?** Только картинки, или с шапкой — проект,
   заказчик, номер договора, — чтобы лист не был безымянным?
2. **Как выбирать?** Отдельная кнопка «Печать визуализации» в разделе, или пункт
   в окне печати рядом с шаблонами?
3. **Два поля — это две области на одном холсте или две отдельные карточки?**
   От этого зависит, переносить ли картинки между ними перетаскиванием.
4. **Что со старыми расчётами?** В сохранённых пресетах картинки лежат одним
   списком без признака вида. Считать их визуализацией, планировкой или
   спрашивать при открытии?
